# Benchmark Report

## Retrieval evaluation results

**Status: Pending live execution.** The reproducible retrieval harness and
frozen `retrieval_cases.v1` input are implemented, but this repository does
not yet contain a live ICRN/PostgreSQL run artifact. Therefore no Recall,
citation-correctness, section-correctness, or “92% relevance” result is
reported here. See [the evaluation plan](evaluation_plan.md) for the rubric and
commands; publish only aggregate values linked to a corresponding
`artifacts/retrieval_eval/<run-id>/run_manifest.json`.
